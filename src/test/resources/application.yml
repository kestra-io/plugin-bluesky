kestra:
  repository:
    type: memory
  queue:
    type: memory
  storage:
    type: local
    local:
      base-path: /tmp/unittest
  url: https://mysuperhost.com/kestra/ui

  worker:
    controllers:
      type: STATIC
      static:
        endpoints:
          - host: localhost
micronaut:
  server:
    port: 59443
